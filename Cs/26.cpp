#include <bits/stdc++.h>

using namespace std;

int main(){
    int N;
    cin >> N;
    vector<int> B(N,1),J(N-1);
    for(int i=0;i<N-1;i++){
        cin >> J[i];
    }

    for(int i=N-2;0<=i;i--){
        
    }
}