#include <bits/stdc++.h>

using namespace std;

int main(){
    long long int N,H,A,B,C,D,E,ans=1e9;
    cin >> N >> H;
    cin >> A >> B >> C >> D >> E;
    for(int i=0;i<N;i++){
        if(A+E)*
    }
}