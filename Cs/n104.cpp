#include <bits/stdc++.h>

using namespace std;

int main(){
    int D,G;
    cin >> D >> G;
    vector<pair<int,int>> p(D)
    for(int i=0;i<D;i++){
        cin >> p[i].first >> p[i].second;
    }

    

}
