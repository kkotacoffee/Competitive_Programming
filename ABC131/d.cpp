#include <bits/stdc++.h>

using namespace std;

int main(){
    int N;
    cin >> N;
    vector<int> a(N),b(N),n(N);
    for(int i=0;i<N;i++){
        cin >> a[i] >> b[i];
        n[i] = 
    }
    return 0;
}