#include <bits/stdc++.h>

using namespace std;

int main(){
    long long int N,K;
    vector<long long int> kbit;
    cin >> N >> K;
    for(int i=0;0<K;i++){
        kbit.push_back()
    }

    vector<long long int> A(N);
    vector<vector<long long int>> bit(N,vector<long long int> (100,0))

    for(int i=0;i<N;i++){
        cin >> A[i];
        for(int j=0;0<A[i];j++){
            bit[i][j] = A[i]%2;
            A[i] /= 2;
        }
    }

    for(int )


}